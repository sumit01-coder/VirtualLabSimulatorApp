<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <View
        android:layout_width="32dp"
        android:layout_height="4dp"
        android:background="@drawable/bg_chip_yellow_soft"
        android:layout_marginBottom="12dp" />

    <ImageView
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:src="@drawable/ic_tickets_24"
        app:tint="@color/warning"
        android:layout_marginBottom="10dp" />

    <TextView
        android:id="@+id/statTickets"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0"
        android:textColor="@color/text_primary"
        android:textSize="26sp"
        android:textStyle="bold"
        android:letterSpacing="-0.02" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Total Tickets"
        android:textColor="@color/text_muted"
        android:textSize="11sp"
        android:layout_marginTop="2dp" />

    <TextView
        android:id="@+id/statTicketsOpen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0 Open"
        android:textColor="@color/brand"
        android:textSize="11sp"
        android:textStyle="bold"
        android:layout_marginTop="6dp"
        android:background="@drawable/bg_chip_blue_soft"
        android:paddingHorizontal="8dp"
        android:paddingVertical="4dp" />

</LinearLayout>
